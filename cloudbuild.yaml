steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [ 'build', '-t', 'us-central1-docker.pkg.dev/ml-etiquetas/ml-etiquetas-repo/app:latest', '.' ]
  env:
    - 'DOCKER_BUILDKIT=1'
images:
- 'us-central1-docker.pkg.dev/ml-etiquetas/ml-etiquetas-repo/app:latest'
